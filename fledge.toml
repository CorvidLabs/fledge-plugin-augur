# fledge.toml: project task definitions
[tasks]
build = "swift build -c release"
test = "bash Tests/plugin_contract_test.sh"
check = "swift build -c release && .build/release/fledge-augur check"

[lanes.verify]
description = "Build the Augur plugin"
steps = ["build", "test"]
